# 72h 工作流约定

## 阶段划分

- **前 6h**：读题（`competition/problem`）、明确任务、分工。
- **6–30h**：数据获取与清洗（`projects/01_data`）+ 文献调研（`reference/literature`）。
- **30–60h**：建模与求解（`projects/02_modeling`），每 2h 一次合流同步。
- **60–68h**：可视化定稿（`projects/03_visualization`）+ 论文（`projects/04_paper`）。
- **最后 4h**：核对 `competition/rules` 提交要求、打包 `results/submission`。

## 分工建议

- A：建模与求解（主）＋ 优化模型
- B：数据 + 可视化
- C：论文 LaTeX + 文献管理 + 合规

## 红线

- `competition/` 只读；引用以 Zotero 为准；AI 不生成核心代码。
